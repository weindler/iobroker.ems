export const PLANNER_PREPARED_INPUT_SCHEMA_VERSION = 1;
export const PLANNER_PREPARED_INPUT_FILE = "prepared_input_v1.json";
export const PLANNER_PREPARED_INPUT_BUDGET_BYTES = 64 * 1024;

export const PREPARATION_REVISION_EXCLUDED_KEYS = ["preparationRevision", "generatedAt"] as const;
